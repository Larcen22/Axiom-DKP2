{
  "name": "axiom-dkp2",
  "version": "1.0.0",
  "private": true,
  "description": "Axiom DKP Metrics — read-only client-side EverQuest raid DKP dashboard (tests)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "papaparse": "^5.4.1",
    "vitest": "^3.2.0"
  }
}

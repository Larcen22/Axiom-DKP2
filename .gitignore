.pi/
.pi/.vscode-ready
node_modules/
test-results/
playwright-report/
# Guild data exports — anchored to repo root so the synthetic test fixtures in
# test/fixtures/sample-data/ (same filenames) can be committed for CI.
/users.json
/transactions.json
/roster-export.csv
/loot.json
/raids.json

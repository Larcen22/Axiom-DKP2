.pi/
.pi/.vscode-ready
node_modules/
test-results/
playwright-report/
# Guild data exports at the repo root (loot.json, raids.json, users.json,
# transactions.json, roster-export.csv) are intentionally committed and pushed —
# do NOT gitignore them. Only rule: never push a stale local export up.
